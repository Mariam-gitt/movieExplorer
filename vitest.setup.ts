// "@testing-library/jest-dom" adds extra, more readable assertions on top
// of Vitest's built-in "expect" — e.g. expect(el).toBeInTheDocument()
// instead of a clunkier manual check. Importing it here (in a file that
// runs before every test, per vitest.config.ts's "setupFiles") makes those
// extra assertions available in every test file automatically.
import "@testing-library/jest-dom/vitest";
